#!/bin/bash
set -e

# Intake & Body Quoter is a single-file app (index.html + server.js) with no
# dependencies to install or migrations to run. Nothing to do after a merge.
echo "Post-merge setup: nothing to install for this project."
